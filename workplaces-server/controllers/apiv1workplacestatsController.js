import * as service from '../services/apiv1workplacestatsService.js';

export function funcapiv1workplacestats(req, res) {
    service.funcapiv1workplacestats(req, res);
}

export function getWorkplace_stats(req, res) {
    service.getWorkplace_stats(req, res);
}

