import * as service from '../services/apiv1workplacestatsyearService.js';

export function funcapiv1workplacestatsyear(req, res) {
    service.funcapiv1workplacestatsyear(req, res);
}

export function findByyear(req, res) {
    service.findByyear(req, res);
}

